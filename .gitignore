# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/
# Stray root lockfile (the Foundry project lives in contracts/; submodule pin is tracked by git)
/foundry.lock

# Node
node_modules/
contracts/node_modules/
dist/
*.log

# Ponder (indexer/) generated artifacts
indexer/.ponder/
indexer/ponder-env.d.ts

# Env / secrets — only .env.example is committed
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
